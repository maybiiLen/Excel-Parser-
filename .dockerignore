# Rebuilt inside the image — never copy these in.
node_modules
.next
out
build
coverage

# VCS & local tooling
.git
.gitignore
.gitattributes
.claude
.vercel

# Docker files themselves
Dockerfile
.dockerignore
docker-compose.yml

# Env & OS cruft
.env*
.DS_Store
npm-debug.log*
*.tsbuildinfo
