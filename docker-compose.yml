# Convenience wrapper: `docker compose up --build` builds and serves the app
# at http://localhost:3000. Used for local runs and the repo-based handoff.
services:
  app:
    build: .
    image: excel-parser:latest
    container_name: excel-parser
    ports:
      - "3000:3000"
    restart: unless-stopped
